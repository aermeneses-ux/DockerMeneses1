name: Build and Publish Docker Image

on:

  push:

    branches:
      - main

permissions:
  contents: read
  packages: write

env:
  IMAGE_NAME: devops-flask-postgres
  IMAGE_TAG: 2.0.0

jobs:

  build:

    runs-on: ubuntu-latest

    steps:

      - name: Descargar Código
        uses: actions/checkout@v4

      - name: Login GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Construir Imagen
        run: |
          docker build \
          -t ghcr.io/aermeneses-ux/dockermeneses1:${{ env.IMAGE_TAG }} \
          -t ghcr.io/aermeneses-ux/dockermeneses1:latest \
          .

      - name: Publicar Imagen
        run: |
          docker push ghcr.io/aermeneses-ux/dockermeneses1:${{ env.IMAGE_TAG }}
          docker push ghcr.io/aermeneses-ux/dockermeneses1:latest